import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { borderTopColor: "white", borderTopWidth: 1, flex: 1 },
  columnWrapper: {
    justifyContent: "space-between",
    backgroundColor: "white",
    borderTopColor: "#fff"
  }
});

export default styles;
